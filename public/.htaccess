Options -Indexes

RewriteEngine On

# Sert les fichiers et dossiers existants tels quels (CSS, images, uploads...)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout le reste → index.php
RewriteRule ^ index.php [QSA,L]
